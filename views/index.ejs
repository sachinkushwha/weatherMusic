<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>WeatherPulse</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@400;600&display=swap" rel="stylesheet" />
    <style>
        body {
            font-family: 'Outfit', sans-serif;
        }
    </style>
</head>

<body class="bg-gradient-to-br from-[#0f2027] via-[#203a43] to-[#2c5364] min-h-screen flex items-center justify-center p-4">

    <div
        class="bg-white/10 backdrop-blur-xl border border-white/20 rounded-3xl p-6 sm:p-8 w-full max-w-sm sm:max-w-md mx-auto shadow-2xl transition hover:scale-[1.02] duration-300 ease-in-out text-white">

        <!-- Header -->
        <div class="text-center mb-8">
            <h1 class="text-4xl font-bold mb-2">🌍 WeatherPulse</h1>
            <p class="text-gray-200 text-sm">Your Real-Time Weather Companion</p>
        </div>

        <!-- Responsive Search Bar -->
        <form action="/" method="post">
            <div class="flex flex-col sm:flex-row bg-white/20 border border-white/30 rounded-xl overflow-hidden mb-6">
                <input type="text" name="city" placeholder="Enter city..."
                    class="w-full px-4 py-3 bg-transparent text-white placeholder-gray-300 focus:outline-none" />
                <button type="submit"
                    class="bg-yellow-400 text-black font-semibold px-5 py-3 hover:bg-yellow-500 transition">
                    Search
                </button>
            </div>
        </form>

        <!-- Weather Info -->
        <div class="space-y-6">
            <div class="text-center">
                <h2 class="text-2xl font-semibold">📍 <%=city%></h2>
                <p class="text-6xl font-bold mt-2">🌤 <%=main.temp%>°C</p>
                <p class="text-lg text-gray-300 mt-1"><%=mosum%></p>
            </div>

            <!-- Stats Grid -->
            <div class="grid grid-cols-3 gap-3 text-sm text-center">
                <div class="bg-white/10 rounded-xl p-3 hover:bg-white/20 transition">
                    <div class="text-xl mb-1">💧</div>
                    <div>Humidity</div>
                    <div class="font-bold text-lg"><%=main.humidity%>%</div>
                </div>
                <div class="bg-white/10 rounded-xl p-3 hover:bg-white/20 transition">
                    <div class="text-xl mb-1">🌬️</div>
                    <div>Wind</div>
                    <div class="font-bold text-lg"><%=wind*3.6%> km/h</div>
                </div>
                <div class="bg-white/10 rounded-xl p-3 hover:bg-white/20 transition">
                    <div class="text-xl mb-1">📈</div>
                    <div>Pressure</div>
                    <div class="font-bold text-lg"><%=main.pressure%> hPa</div>
                </div>
            </div>
        </div>

    </div>

</body>

</html>
